integer_random= random.randint(1, 25)
print("your randon is "+str(integer_random))
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
//main.py

import random
import my_module
random_integer=random.randint(1,15)
print(random_integer)

print(my_module.pi)

my_module.py
pi=3.14159246
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
import random
random_integer=random.randint(1,10)
#print(random_integer)

random_float= random.random()*5
print(random_float)

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
import random
random_integer=random.randint(1,10)
#print(random_integer)

random_float= random.random()*5
print(random_float)

love_score= random.randint(1,100)
print(f"Your love score is {love_score} ")
-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Search in askpython.com
Challenge
Heads or Tails
Instructions
You are going to write a virtual coin toss program. It will randomly tell the user "Heads" or "Tails".
Important, the first letter should be capitalised and spelt exactly like in the example e.g. Heads, not heads.
There are many ways of doing this. But to practice what we learnt in the last lesson, you should generate a random number, either 0 or 1. Then use that number to print out Heads or Tails.
-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
e.g.
1 means Heads
0 means Tails

import random
head_tail= random.randint(0,1)
if head_tail==0:
  print(f"you have picked {head_tail}, is a head ")
else:
  print(f"you have picked {head_tail}., is a tail")
  
  --------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  
  
  
