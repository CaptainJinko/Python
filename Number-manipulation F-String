//the formula is weight/height sqaurw
height=float(input("Enter your height in m: "))
weight=float(input("Enter your weight in kg: "))

answerOfBmi=int(weight)/float(height)**2
print(float(answerOfBmi))

#Converting it into whole number int 
-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
height=float(input("Enter your height in m: "))
weight=float(input("Enter your weight in kg: "))

Bmi=int(weight)/float(height)**2
Bmi_as_int= int(Bmi)
print( Bmi_as_int )
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# 🚨 Don't change the code below 👇
height = input("enter your height in m: ")
weight = input("enter your weight in kg: ")
# 🚨 Don't change the code above 👆

#Write your code below this line 👇

weight_as_int = int(weight)
height_as_float = float(height)

# Using the exponent operator **
bmi = weight_as_int / height_as_float ** 2
# or using multiplication and PEMDAS
bmi = weight_as_int / (height_as_float * height_as_float)

bmi_as_int = int(bmi)

print(bmi_as_int)
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
height=float(input("Enter your height in m: "))
weight=float(input("Enter your weight in kg: "))

Bmi=int(weight)/float(height)**2
Bmi_as_int= int(Bmi)
print("your mbi is "+str(Bmi))
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

#Number maipulation and F String in Python

#print(8/3)          #Result 2.666666666665
#print(8//3)            # 3 without having to convert it to interger
#print(int(8/3))     #2
#print(round(8/3))   #3
#print(round(8/3, 2))  # 2.67 it will be rounded by two dicimal places 

result=(4/2)
# print(result) 
# result= result/2  # short form: result/=2, result-=2, result+=7
# print(result)
# result+=7
# print(result)

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
#challenge
#Welcome to the tip calculator
#Waht was the total bill? 124.56
#What percentage would like to give? 10, 12, or 15? 
# How many people to split the bill 7
#Each person should pay: $19:93
#
solution to challenge
#first optin

print("Welcome to the tip calculator!")
bill=float(input("what was the total bill? $ " ))
tip=int(input("what percentage tip you like to give? 10, 12, 15, or 12?"))
people= int(input("Howe many people to split the bill"))
bill_with_tip=tip/100*bill+bill
print(bill_with_tip)
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------
#second option 
print("Welcome to the tip calculator!")
bill=float(input("What was the total bill? $ "))
tip=int(input("What percentage would like to give 10, 12, or 15?"))
people=int(input("How many people to split the bill?")) 
bill_with_tip=bill*(1+tip/100)
print(f"Your tip with bill is :{bill_with_tip} ")
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
#third option 
print("Welcome to the tip calculator!")
bill=float(input("What was the total bill? $ "))
tip=int(input("What percentage would like to give 10, 12, or 15?"))
people=int(input("How many people to split the bill?")) 
tip_as_percent=tip/100
total_tip_amount=bill*tip_as_percent
total_bill=bill+total_tip_amount
bill_per_person= total_bill/people
final_bill=round(bill_per_person ,2)
print(f"The final bill is $ {final_bill} ")


